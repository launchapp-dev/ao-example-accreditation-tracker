mcp_servers:
  filesystem:
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "/Users/samishukri/ao-examples/examples/accreditation-tracker"

  sequential-thinking:
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-sequential-thinking"

  playwright:
    command: npx
    args:
      - "-y"
      - "@playwright/mcp"
