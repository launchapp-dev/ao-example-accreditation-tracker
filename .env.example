# Accreditation Tracker — Environment Variables
# No API keys required for the core pipeline.
# All processing uses Claude models via the AO daemon.
# MCP servers (filesystem, sequential-thinking, playwright) install automatically via npx.

# Optional: Override MCP filesystem path if running from a different location
# AO_FILESYSTEM_ROOT=/path/to/accreditation-tracker
